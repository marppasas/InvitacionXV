import { NgModule } from "@angular/core";
import { AsistProxyService } from "./services/asist.proxy.service";

@NgModule({
    providers: [
        AsistProxyService,
    ]
})
export class ApiProxyModule {

}